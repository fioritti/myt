spring:
  datasource:
    url: jdbc:mysql://localhost:3306/spring_social?useSSL=fasle
    username: root
    password: callicoder
     
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect   
      